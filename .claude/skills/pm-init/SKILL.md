---
description: Deprecated. Use /ctx-context-init instead.
---

# PM Init — Deprecated

This skill has been replaced by `/ctx-context-init`, which scaffolds the
new `context/` + `tasks/` layout and sets up Notion routing config.

Run `/ctx-context-init` to initialize a new consumer repo.
