---
description: Deprecated. Use /context-init instead.
---

# PM Init — Deprecated

This skill has been replaced by `/context-init`, which scaffolds the
new `context/` + `tasks/` layout and sets up Notion routing config.

Run `/context-init` to initialize a new consumer repo.
